// slice function
const text = 'I like Javascript';
const result = text.slice(4, 8); // (first index - included, last index - not included)

console.log(result);

// substring function
const textSub = 'I love Javascript';
const resultsub = textSub.substr(7,10);

console.log(resultsub);

//split function
let friends = 'tute, mati, divya';
let friendsArray = friends.split(',');

console.log(friendsArray);

